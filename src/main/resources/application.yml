spring:
  application:
    name: OneVerse
  ai:
    google:
      genai:
        api-key: ${GOOGLE_API_KEY}
        chat:
          options:
            model: "gemini-3.1-flash-lite"
            temperature: 0.7